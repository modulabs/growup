services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: growup
      POSTGRES_USER: growup
      POSTGRES_PASSWORD: growup_dev
    ports:
      - "5432:5432"
    volumes:
      - growup_data:/var/lib/postgresql/data

volumes:
  growup_data:
